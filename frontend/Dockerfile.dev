# Development Dockerfile for Vite dev server with HMR
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package*.json ./
RUN npm ci

# Source code is mounted via volume, not copied
# This enables hot-reload without rebuilding

EXPOSE 5173

# Run Vite dev server
CMD ["npm", "run", "dev"]
